# Build artifacts
/target/
examples/wasi_capability_demo/guest/target/
**/*.rs.bk

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Temporary files
*.tmp
*.bak
*.old

# Cargo lock (keep for reproducible builds)
# Cargo.lock

# Environment files
.env
.env.local

# MCP server config (contains token references)
.mcp.json

# Debug files
*.pdb
*.dSYM/

# Benchmark artifacts (generated by CI and local runs)
/artifacts/
/dashboard/out/

# Local AI agent harness — developer tooling for the models worked with on this
# checkout (Claude Code, Codex, Cursor, etc.). Kept on disk but out of the repo:
# it is not part of the Nexus product and differs per developer/machine.
.claude/
.codex/
.agents/
.cursor/
/AGENTS.md
/PRPs/
# Fuzzing build/run artifacts (fuzz/ is a separate workspace; Cargo.toml/Cargo.lock/fuzz_targets stay tracked)
fuzz/target/
fuzz/corpus/
fuzz/artifacts/
fuzz/crashes/
