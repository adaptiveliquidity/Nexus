# Phase B A/B: without vs with InstinctPolicy

Generated by `cargo run --release --example instinct_ab`.

Each scenario was executed twice through `NexusHypervisor::execute_tool`:
once with `StaticPolicy` only, and once with `LayeredPolicy([Static, InstinctPolicy])` after seeding the instinct store with 100 simulated successful retries per failure mode.

## infinite_loop

### Without InstinctPolicy
  1. [Static, conf=1.00] Fuel budget of 10000000 instructions was consumed; the guard worked as designed.
  2. [Static, conf=0.90] Profile the WASM to find the hot loop, then either reduce its iteration count or raise the per-tool fuel cap with an explicit business rationale.
  3. [Static, conf=0.70] Consider iterative or memoized algorithms instead of naive recursion/iteration over large inputs.

### With InstinctPolicy (after 100 simulated successes)
  1. [Static, conf=1.00] Fuel budget of 10000000 instructions was consumed; the guard worked as designed.
  2. [Static, conf=0.90] Profile the WASM to find the hot loop, then either reduce its iteration count or raise the per-tool fuel cap with an explicit business rationale.
  3. [Static, conf=0.70] Consider iterative or memoized algorithms instead of naive recursion/iteration over large inputs.

_Static actions: 3 -> 3 (added by instinct: 0)_

## trap_unreachable

### Without InstinctPolicy
  1. [Static, conf=1.00, non-retryable] Deterministic `unreachable` instruction reached — likely an assertion failure or unhandled enum arm in the guest module.
  2. [Static, conf=0.90, non-retryable] Locate the failing instruction via the wasm backtrace (function index + offset). Fix the guest code or the invariant it expected.
  3. [Static, conf=1.00, non-retryable] Do NOT auto-retry with identical inputs; this failure is deterministic.

### With InstinctPolicy (after 100 simulated successes)
  1. [Static, conf=1.00, non-retryable] Deterministic `unreachable` instruction reached — likely an assertion failure or unhandled enum arm in the guest module.
  2. [Static, conf=0.90, non-retryable] Locate the failing instruction via the wasm backtrace (function index + offset). Fix the guest code or the invariant it expected.
  3. [Static, conf=1.00, non-retryable] Do NOT auto-retry with identical inputs; this failure is deterministic.
  4. [Instinct, conf=0.99] [instinct] team-X playbook: assertion failures correlate with empty input list

_Static actions: 3 -> 4 (added by instinct: 1)_

## div_by_zero

### Without InstinctPolicy
  1. [Static, conf=1.00, non-retryable] Integer divide-by-zero — guard the divisor (`if denom == 0 { return … }`) or use checked-division at the call site.
  2. [Static, conf=0.90, non-retryable] Audit the input contract that allowed a zero divisor through; validation at the boundary is the right fix.
  3. [Static, conf=1.00, non-retryable] Do NOT auto-retry with identical inputs; this failure is deterministic.

### With InstinctPolicy (after 100 simulated successes)
  1. [Static, conf=1.00, non-retryable] Integer divide-by-zero — guard the divisor (`if denom == 0 { return … }`) or use checked-division at the call site.
  2. [Static, conf=0.90, non-retryable] Audit the input contract that allowed a zero divisor through; validation at the boundary is the right fix.
  3. [Static, conf=1.00, non-retryable] Do NOT auto-retry with identical inputs; this failure is deterministic.
  4. [Instinct, conf=0.99] [instinct] team-X playbook: always check denom from API boundary

_Static actions: 3 -> 4 (added by instinct: 1)_

## stack_overflow

### Without InstinctPolicy
  1. [Static, conf=1.00, non-retryable] Call stack exhausted — convert the recursive function to iteration, add an explicit depth bound, or restructure into trampolines.
  2. [Static, conf=0.85, non-retryable] Reject modules with unbounded recursion at validation; only legitimately deep, bounded workloads should raise the stack limit.

### With InstinctPolicy (after 100 simulated successes)
  1. [Static, conf=1.00, non-retryable] Call stack exhausted — convert the recursive function to iteration, add an explicit depth bound, or restructure into trampolines.
  2. [Static, conf=0.85, non-retryable] Reject modules with unbounded recursion at validation; only legitimately deep, bounded workloads should raise the stack limit.
  3. [Instinct, conf=1.00] [instinct] team-X playbook: convert this recursion via accumulator + tail call

_Static actions: 2 -> 3 (added by instinct: 1)_

## memory_out_of_bounds

### Without InstinctPolicy
  1. [Static, conf=1.00, non-retryable] Out-of-bounds linear-memory access — bounds-check the index before the load/store or use a memory.size guard.
  2. [Static, conf=0.85, non-retryable] Inspect the guest allocator for an off-by-one; ensure memory.grow happens before any access beyond the original heap.

### With InstinctPolicy (after 100 simulated successes)
  1. [Static, conf=1.00, non-retryable] Out-of-bounds linear-memory access — bounds-check the index before the load/store or use a memory.size guard.
  2. [Static, conf=0.85, non-retryable] Inspect the guest allocator for an off-by-one; ensure memory.grow happens before any access beyond the original heap.
  3. [Instinct, conf=0.99] [instinct] team-X playbook: this module needs 4 pages min, set memory_pages=4

_Static actions: 2 -> 3 (added by instinct: 1)_

## Summary

- Scenarios tested: 5
- Total instinct-sourced actions added: 4
- Avg additional actions per scenario: 0.8

The added actions are tagged `source: "Instinct"` and carry the Bayes-smoothed confidence from the seeded support count (101 / 102 ≈ 0.99). LayeredPolicy de-dupes by description before returning, so the count above is purely additive — no Static actions were displaced.
